<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="helpdesk_stage_search" model="ir.ui.view">
        <field name="name">Estados - Búsqueda</field>
        <field name="model">helpdesk_lite.stage</field>
        <field name="arch" type="xml">
            <search string="Estados Búsqueda">
                <field name="name"/>
                <field name="sequence"/>
            </search>
        </field>
    </record>

    <record id="helpdesk_stage_tree" model="ir.ui.view">
        <field name="name">pyme_helpdesk.stage.tree</field>
        <field name="model">helpdesk_lite.stage</field>
        <field name="arch" type="xml">
            <tree string="Estados">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="helpdesk_stage_form" model="ir.ui.view">
        <field name="name">pyme_helpdesk.stage.form</field>
        <field name="model">helpdesk_lite.stage</field>
        <field name="priority" eval="1"/>
        <field name="arch" type="xml">
            <form string="Estados">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Nombre del Estado..."/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="last"/>
                            <field name="fold"/>
                        </group>
                        <group>
                        </group>
                    </group>
                    <separator string="Requisitos"/>
                    <field name="requirements" nolabel="1"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="helpdesk_stage_action" model="ir.actions.act_window">
        <field name="name">Estados</field>
        <field name="res_model">helpdesk_lite.stage</field>
        <field name="view_type">form</field>
        <field name="view_id" ref="pyme_helpdesk.helpdesk_stage_tree"/>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                 Haga clic para establecer una nueva etapa en su canal de soporte.
            </p><p> 
                 Los estados permiten al soporte rastrear fácilmente cómo un ticket específico
            se posiciona en el ciclo de soporte
        </p>
        </field>
    </record>

    <menuitem
            id="menu_helpdesk_stage_act"
            name="Estados"
            sequence="20"
            action="helpdesk_stage_action"
            parent="menu_config"
            groups="base.group_no_one"/>

</odoo>
